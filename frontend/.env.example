# Google OAuth Configuration
# Get these from Google Cloud Console - https://console.cloud.google.com
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Debug mode for OAuth (optional) - set to 'true' to enable console logging
VITE_DEBUG_OAUTH=false

# Instructions:
# 1. Go to Google Cloud Console
# 2. Create a new project or select existing
# 3. Enable OAuth 2.0 (no additional APIs needed for basic auth)
# 4. Create OAuth 2.0 Client ID credentials
# 5. Add your domain to authorized origins
# 6. Add callback URL: http://localhost:5173/auth/google/callback (for dev)
# 7. Copy Client ID and Client Secret to this file
